# Notebooks

Notebooks are for inspection, diagnostics and presentation.

Core data processing, feature generation, labelling, modelling and validation
logic lives in the `chronoslob` package so it can be tested, reviewed and
reused.

Clear notebook outputs before committing, or regenerate them from reproducible
experiment artefacts.
